# Hugging Face token (required for gated models like Mistral)
HF_TOKEN=hf_xxx

# OpenRouter API key (only needed for metrics_finder LLM-judge evaluation)
OPENROUTER_API_KEY=sk-or-xxx

# OpenAI API key (only needed for eval_bundle semantic entropy with GPT)
OPENAI_API_KEY=sk-xxx
