# HuggingFace token — required on the server for gated models (Mistral, Llama).
HF_TOKEN=hf_xxx

# OpenRouter API key — required on the server for the LLM-as-judge stage.
OPENROUTER_API_KEY=sk-or-xxx
