# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs / env files
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/

# Pipeline artifacts (big, live on server) — anchored to repo root only,
# so that `src/sae_muc/data/` (the dataset loaders package) stays tracked.
/data/
*.pt
*.pkl
*.npy
*.pickle
*.h5
*.safetensors

# Reference materials kept local only
Calibrating_Verbal_Uncertainty_as_a_Linear_Feature_to_Reduce_Hallucinations.pdf
project_report (1)/

# Per-developer instructions for Claude Code (kept local)
CLAUDE.md
